[project]
name = "openmontage-studio-api"
version = "0.1.0"
description = "Local read-mostly API for OpenMontage Studio"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
]

[project.optional-dependencies]
dev = []
