import { PlaceholderPage } from "@/components/placeholder-page";

export default function CapabilitiesPage() {
  return (
    <PlaceholderPage
      title="Capabilities"
      description="Browse discovered tools, providers, and pipeline entry points from the repo registry."
      phase="Phase 1"
    />
  );
}
