import { PlaceholderPage } from "@/components/placeholder-page";

export default function SetupPage() {
  return (
    <PlaceholderPage
      title="Setup"
      description="Configure provider keys, local runtimes, and preflight checks before production."
      phase="Phase 1"
    />
  );
}
