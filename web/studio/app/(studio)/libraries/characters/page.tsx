import { PlaceholderPage } from "@/components/placeholder-page";

export default function CharactersLibraryPage() {
  return (
    <PlaceholderPage
      title="Characters"
      description="Browse and manage reusable character rigs and reference assets."
      phase="Phase 5"
    />
  );
}
